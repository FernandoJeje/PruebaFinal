const CrearUsuario = () => {
  const onSubmitHandler = () => {};

  return (
    <form onSubmit={onSubmitHandler}>
      <input type="text"></input>
      <input type="text"></input>
    </form>
  );
};

export default CrearUsuario;
